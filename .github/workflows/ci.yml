name: Front CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./front

    steps:
      
      - name: 🔄 Checkout du code
        uses: actions/checkout@v4

    #  - name: 🔄 Mettre à jour les actions
    #    run: sudo apt-get update && sudo apt-get upgrade -y


      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: front/package-lock.json

      - name: 📦 Installer les dépendances
        run: npm install

      # - name: ✅ Lancer les tests
      #   run: npm test -- --watchAll=false


      

      - name: 🐳 Connexion à DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: 🐳 Build de l'image Docker
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/frontend-front:latest .

      - name: 📤 Push de l'image Docker
        run: docker push ${{ secrets.DOCKER_USERNAME }}/frontend-front:latest


      
      - name: ⚙️ Build de l'app
        run: npx tsc && npm run build


      - name: 📤 Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: ./front/dist
